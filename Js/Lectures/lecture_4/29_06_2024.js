/* 
    miscellaneous operators
    Bitwise operator
    logical operator
*/

/* Miscellaneous Operator */

/* Ternary Operator */

//  ? :

{
    // let age = 17;
    // let result = age >= 18 ? 'do it vote' : 'do not vote';

    // console.log(result);
}

// , (comma operator)

{
    // let x = (10 , 20 , 30);
    // console.log(x);

    // a = 20 , b = 30 , c = 40;

    // a = 20;
    // b = 30;
    // c = 40;
}

// typeof

{
    // let x = 20;
    // let y = 1000n;
    // let z = null;

    // console.log(typeof (x));
    // console.log(typeof y);
    // console.log(typeof z);

}

/* Bitwise Operator */

// Bitwise And()

// When a bitwise And is performed on a pair of bits, it returns 1 if both bits are 1.

{
    // let x = 1;
    // let y = 0;

    // console.log(x & y);
    // console.log(y & x);
    // console.log(x & x);
    // console.log(y & y);
}

// Bitwise OR()

// when a bitwise OR is performed on a pair of bits, it returns 1 if one of the bits is 1.

{
    // let x = 1;
    // let y = 0;

    // console.log(x | y);
    // console.log(y | x);
    // console.log(x | x);
    // console.log(y | y);
}

// Bitwise XOR()

// when a bitwise XOR is performed on a pair of bits, it retutrns 1 if the bits are different.

{
    // let x = 1;
    // let y = 0;
    
    // console.log(x ^ y);
    // console.log(y ^ x);
    // console.log(x ^ x);
    // console.log(y ^ y);
}

/* Logical Operator */

// Logical And

{
    let x = false;
    let y = false;
    let z = x & y;

    console.log(z);
}

// Logical OR

{
    // let x = true;
    // let y = false;
    // let z = x || y;
    
    // console.log(z);
}

// Logical NOT

{
    // let x = true;
    // let y = false;
    // let z = !(y);

    // console.log(z);

    // let z1 = !(true && false) + (false && true);

    // console.log(z1);
    
}